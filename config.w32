// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("arrayutils", "for arrayutils support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("arrayutils", "enable arrayutils support", "no");

if (PHP_ARRAYUTILS != "no") {
	EXTENSION("arrayutils", "arrayutils.c");
}

